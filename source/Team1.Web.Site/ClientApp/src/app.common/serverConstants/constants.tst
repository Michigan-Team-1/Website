${
    using Typewriter.Extensions.Types;
}
    $Classes(PolicyNames)[
      $Constants[
        export const $name:string = `$Value`;
      ]
    ]
     $Classes(Role)[
      $Constants[
        export const $name:string = `$Value`;
      ]
    ]
     $Classes(AllowedFileExtensions)[
    export class allowedFileExtensions{
      $Constants[
        public static $name:string = `$Value`;
      ]
       }
    ]
